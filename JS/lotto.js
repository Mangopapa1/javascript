Math.floor(Math.random()*5); //2 ~ 6

Math.floor(Math.random()*5+2); // 2 ~ 7

function randomNum(min, max){
    return Math.floor(Math.random()*(max-min)+min);
}

let rotto = new Set();
while(rotto.size < 6) {
    rotto.add()
}